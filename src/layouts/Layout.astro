---
interface Props {
	title: string;
	description?: string;
}

const { title, description = "Family-Owned and -Operated Heating & Air Services in Bastrop, TX" } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700;800;900&family=Dancing+Script:wght@400;500;600;700&display=swap" rel="stylesheet">
		<title>{title}</title>

		<!-- Schema.org Structured Data -->
		<script type="application/ld+json" is:inline>
		{
			"@context": "https://schema.org",
			"@type": "HVACBusiness",
			"name": "String Prairie Heating & Air",
			"image": "https://stringprairieheatingandair.com/logo.png",
			"@id": "https://stringprairieheatingandair.com",
			"url": "https://stringprairieheatingandair.com",
			"telephone": "(512) 924-9744",
			"address": {
				"@type": "PostalAddress",
				"streetAddress": "Rosanky",
				"addressLocality": "Rosanky",
				"addressRegion": "TX",
				"postalCode": "78953",
				"addressCountry": "US"
			},
			"geo": {
				"@type": "GeoCoordinates",
				"latitude": 29.8918229,
				"longitude": -97.3320232
			},
			"openingHoursSpecification": {
				"@type": "OpeningHoursSpecification",
				"dayOfWeek": [
					"Monday",
					"Tuesday",
					"Wednesday",
					"Thursday",
					"Friday"
				],
				"opens": "08:00",
				"closes": "17:00"
			},
			"sameAs": [
				"https://www.google.com/maps/place/String+Prairie+Heating+%26+Air/@29.8918229,-97.3320232"
			],
			"priceRange": "$$",
			"areaServed": [
				{
					"@type": "City",
					"name": "Bastrop",
					"@id": "https://en.wikipedia.org/wiki/Bastrop,_Texas"
				},
				{
					"@type": "City",
					"name": "Rosanky"
				},
				{
					"@type": "City",
					"name": "Smithville"
				},
				{
					"@type": "City",
					"name": "Elgin"
				},
				{
					"@type": "City",
					"name": "Paige"
				}
			],
			"hasOfferCatalog": {
				"@type": "OfferCatalog",
				"name": "HVAC Services",
				"itemListElement": [
					{
						"@type": "Offer",
						"itemOffered": {
							"@type": "Service",
							"name": "AC Installation",
							"description": "Professional air conditioning installation services"
						}
					},
					{
						"@type": "Offer",
						"itemOffered": {
							"@type": "Service",
							"name": "AC Repair",
							"description": "Expert air conditioning repair services"
						}
					},
					{
						"@type": "Offer",
						"itemOffered": {
							"@type": "Service",
							"name": "Heating Installation",
							"description": "Professional heating system installation"
						}
					},
					{
						"@type": "Offer",
						"itemOffered": {
							"@type": "Service",
							"name": "Heating Repair",
							"description": "Expert heating system repair services"
						}
					},
					{
						"@type": "Offer",
						"itemOffered": {
							"@type": "Service",
							"name": "HVAC Maintenance",
							"description": "Regular maintenance and tune-ups for HVAC systems"
						}
					}
				]
			}
		}
		</script>
	</head>
	<body>
		<nav class="navbar">
			<div class="container">
			<a href="/" class="logo">
				<span class="logo-text">String Prairie</span>
				<span class="logo-subtitle">Heating & Air</span>
			</a>
				<button class="mobile-menu-toggle" aria-label="Toggle menu">
					<span></span>
					<span></span>
					<span></span>
				</button>
				<ul class="nav-links">
					<li><a href="/">Home</a></li>
					<li><a href="/services">Services</a></li>
					<li><a href="/about">About</a></li>
					<li><a href="/blog">Blog</a></li>
					<li><a href="/faq">FAQ</a></li>
					<li><a href="/contact">Contact</a></li>
				</ul>
				<a href="tel:5129249744" class="cta-button">Call (512) 924-9744</a>
			</div>
		</nav>

		<!-- Sticky Book Now Button -->
		<a href="https://book.housecallpro.com/book/String-Prairie-Heating--Air/df82ded280ce43f4a9ee6ee6d36aef76?v2=true" target="_blank" rel="noopener noreferrer" class="sticky-book-btn" aria-label="Book online">
			📅 Book Now
		</a>

		<main>
			<slot />
		</main>

		<footer>
			<div class="container">
				<div class="footer-content">
					<div class="footer-section">
						<h3>String Prairie Heating & Air</h3>
						<p>Family-owned HVAC mechanical contractor specializing in residential and commercial services.</p>
					</div>
					<div class="footer-section">
						<h4>Contact</h4>
						<p>Rosanky, TX 78953</p>
						<p>Phone: <a href="tel:5129249744">(512) 924-9744</a></p>
					</div>
					<div class="footer-section">
						<h4>Service Area</h4>
						<p>Bastrop County & Surrounding Areas</p>
					</div>
				</div>
				<div class="footer-bottom">
					<p>&copy; {new Date().getFullYear()} String Prairie Heating & Air. All rights reserved.</p>
				</div>
			</div>
		</footer>
	</body>
</html>

<style is:global>
	:root {
		--primary-blue: #295e93;
		--dark-blue: #0b3a69;
		--accent-red: #22c55e;
		--dark-red: #16a34a;
		--text-dark: #1f2937;
		--text-light: #6b7280;
		--bg-light: #f9fafb;
		--white: #ffffff;
	}

	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	body {
		font-family: 'Outfit', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
		line-height: 1.6;
		color: var(--text-dark);
		font-weight: 300;
		font-size: 16px;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
	}

	.navbar {
		background: var(--white);
		box-shadow: 0 4px 4px rgba(0,0,0,0.1);
		position: sticky;
		top: 0;
		z-index: 100;
	}

	.navbar .container {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1.25rem 20px;
	}

	.logo {
		text-decoration: none;
		display: flex;
		flex-direction: column;
	}

	.logo-text {
		font-family: 'Brush Script MT', cursive;
		font-size: 2.25rem;
		font-weight: 400;
		color: var(--primary-blue);
		line-height: 1.2;
		text-transform: none;
		letter-spacing: 0px;
	}

	.logo-subtitle {
		font-size: 0.875rem;
		color: var(--text-light);
		font-weight: 400;
		text-transform: uppercase;
		letter-spacing: 1px;
	}

	.nav-links {
		display: flex;
		list-style: none;
		gap: 2.5rem;
	}

	.nav-links a {
		text-decoration: none;
		color: var(--text-dark);
		font-weight: 600;
		font-size: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		transition: color 0.3s;
		position: relative;
		padding-bottom: 4px;
	}

	.nav-links a::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 2px;
		background: var(--primary-blue);
		transform: scaleX(0);
		transform-origin: right;
		transition: transform 0.3s ease;
	}

	.nav-links a:hover::after {
		transform: scaleX(1);
		transform-origin: left;
	}

	.nav-links a:hover {
		color: var(--primary-blue);
	}

	.cta-button {
		background: linear-gradient(107deg, var(--primary-blue) -70.76%, var(--dark-blue) 87.63%);
		color: var(--white);
		padding: 1rem 2rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		font-size: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		transition: all 0.3s;
		box-shadow: 0 4px 8px rgba(41, 94, 147, 0.3);
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.cta-button::before {
		content: '📞';
		font-size: 1.2rem;
	}

	.cta-button:hover {
		box-shadow: 0 6px 12px rgba(41, 94, 147, 0.4);
		transform: translateY(-2px);
	}

	main {
		min-height: 60vh;
	}

	footer {
		background: linear-gradient(135deg, var(--dark-blue) 0%, var(--primary-blue) 100%);
		color: var(--white);
		padding: 4rem 0 2rem;
		margin-top: 0;
	}

	.footer-content {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 3rem;
		margin-bottom: 2rem;
	}

	.footer-section h3 {
		font-family: 'Brush Script MT', cursive;
		margin-bottom: 1rem;
		color: var(--white);
		font-weight: 400;
		font-size: 2rem;
		text-transform: none;
		letter-spacing: 0px;
	}

	.footer-section h4 {
		margin-bottom: 1rem;
		color: var(--white);
		font-weight: 700;
		font-size: 1.1rem;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	.footer-section p {
		color: rgba(255, 255, 255, 0.9);
		margin-bottom: 0.75rem;
		font-weight: 300;
		line-height: 1.8;
	}

	.footer-section a {
		color: var(--white);
		text-decoration: none;
		font-weight: 600;
		transition: opacity 0.3s;
	}

	.footer-section a:hover {
		opacity: 0.8;
	}

	.footer-bottom {
		border-top: 1px solid rgba(255, 255, 255, 0.2);
		padding-top: 1.5rem;
		text-align: center;
		color: rgba(255, 255, 255, 0.8);
		font-weight: 300;
	}

	.mobile-menu-toggle {
		display: none;
		flex-direction: column;
		gap: 5px;
		background: none;
		border: none;
		cursor: pointer;
		padding: 0.5rem;
		z-index: 101;
	}

	.mobile-menu-toggle span {
		width: 25px;
		height: 3px;
		background: var(--primary-blue);
		transition: all 0.3s ease;
		border-radius: 2px;
	}

	.mobile-menu-toggle.active span:nth-child(1) {
		transform: rotate(45deg) translate(7px, 7px);
	}

	.mobile-menu-toggle.active span:nth-child(2) {
		opacity: 0;
	}

	.mobile-menu-toggle.active span:nth-child(3) {
		transform: rotate(-45deg) translate(7px, -7px);
	}

	.sticky-book-btn {
		position: fixed;
		bottom: 20px;
		right: 20px;
		background: radial-gradient(circle, var(--accent-red) 0%, var(--dark-red) 100%);
		color: white;
		padding: 1rem 1.75rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 700;
		font-size: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		box-shadow: 0 4px 20px rgba(34, 197, 94, 0.4);
		z-index: 99;
		transition: all 0.3s;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.sticky-book-btn:hover {
		transform: translateY(-3px);
		box-shadow: 0 6px 24px rgba(34, 197, 94, 0.5);
	}

	@media (max-width: 768px) {
		.mobile-menu-toggle {
			display: flex;
		}

		.nav-links {
			position: fixed;
			top: 0;
			right: -100%;
			width: 280px;
			height: 100vh;
			background: var(--white);
			flex-direction: column;
			padding: 6rem 2rem 2rem;
			box-shadow: -4px 0 20px rgba(0,0,0,0.1);
			transition: right 0.3s ease;
			z-index: 100;
			gap: 0;
		}

		.nav-links.active {
			right: 0;
		}

		.nav-links li {
			width: 100%;
			border-bottom: 1px solid rgba(41, 94, 147, 0.1);
		}

		.nav-links a {
			display: block;
			padding: 1rem 0;
		}

		.cta-button {
			font-size: 0.875rem;
			padding: 0.75rem 1.5rem;
		}

		.sticky-book-btn {
			font-size: 0.875rem;
			padding: 0.875rem 1.5rem;
		}
	}
</style>

<script>
	// Mobile menu toggle
	const mobileToggle = document.querySelector('.mobile-menu-toggle');
	const navLinks = document.querySelector('.nav-links');

	if (mobileToggle && navLinks) {
		mobileToggle.addEventListener('click', () => {
			mobileToggle.classList.toggle('active');
			navLinks.classList.toggle('active');
		});

		// Close menu when clicking nav links
		navLinks.querySelectorAll('a').forEach(link => {
			link.addEventListener('click', () => {
				mobileToggle.classList.remove('active');
				navLinks.classList.remove('active');
			});
		});

		// Close menu when clicking outside
		document.addEventListener('click', (e) => {
			const target = e.target as Node;
			if (!navLinks.contains(target) && !mobileToggle.contains(target)) {
				mobileToggle.classList.remove('active');
				navLinks.classList.remove('active');
			}
		});
	}
</script>
